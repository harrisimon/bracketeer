const initialDisplayState = {
  unidirectional: true,
  numberOfColumns: 0,
  displaySettings: {
    gridTemplateColumns: `repeat(4, 1fr)`,
    columnGap: '10%',
  },
};

export default initialDisplayState;
